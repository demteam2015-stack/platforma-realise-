
# Описание файлов проекта

Это простое Next.js приложение. Ниже приведено описание каждого файла в проекте.

## Файлы в корневом каталоге

* `package.json`: Этот файл содержит список зависимостей проекта и скрипты для запуска.
* `package-lock.json`: Этот файл гарантирует, что каждый раз устанавливаются одни и те же зависимости.
* `postcss.config.mjs`, `tailwind.config.ts`: Эти файлы предназначены для настройки Tailwind CSS.
* `tsconfig.json`: Этот файл предназначен для настройки TypeScript.
* `README.md`: Этот файл содержит документацию по проекту.

## Файлы в каталоге `src/app`

* `favicon.ico`: Это иконка, которая отображается на вкладке браузера.
* `globals.css`: Этот файл предназначен для глобальных стилей CSS.
* `layout.tsx`: Это основной макет приложения.
* `page.tsx`: Это главная страница приложения.

---

## Шаг 1: Создаём проект (пошагово, с нуля)

🎯 **Цель:** Создать новый, чистый проект на Next.js — с TypeScript, Tailwind и правильной структурой папок.

### 1. Выполнение команды

Откройте терминал и выполните следующую команду. Она запустит интерактивный установщик для создания нового проекта Next.js.

```bash
npx create-next-app@latest
```

### 2. Ответы на вопросы установщика

В процессе установки вам будут заданы несколько вопросов. Отвечайте на них следующим образом, чтобы получить проект, идентичный нашему:

*   **What would you like to name your project?**
    *   Нажмите `Enter`. По умолчанию будет использовано имя папки (например, `demplatform`).

*   **Would you like to use TypeScript?**
    *   Выберите `Yes` и нажмите `Enter`.

*   **Would you like to use ESLint?**
    *   Выберите `Yes` и нажмите `Enter`.

*   **Would you like to use Tailwind CSS?**
    *   Выберите `Yes` и нажмите `Enter`.

*   **Would you like to use `src/` directory?**
    *   ✅ **Важно:** Выберите `Yes` и нажмите `Enter`. Это создаст правильную структуру `src/app`, а не `app` в корне.

*   **Would you like to use App Router? (recommended)**
    *   Нажмите `Enter` (по умолчанию выбрано `Yes`).

*   **Would you like to customize the default import alias (@/*)?**
    *   Выберите `Yes` и нажмите `Enter`.

*   **What import alias would you like configured?**
    *   Нажмите `Enter`. Мы оставляем псевдоним по умолчанию — `@/*`.

После ответов на эти вопросы установщик автоматически создаст папку с вашим проектом, установит все необходимые зависимости и настроит базовую структуру.
